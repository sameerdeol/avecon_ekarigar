import { ComponentFixture, TestBed } from '@angular/core/testing';
import { CurrentTripHelpSupportPage } from './current-trip-help-support.page';

describe('CurrentTripHelpSupportPage', () => {
  let component: CurrentTripHelpSupportPage;
  let fixture: ComponentFixture<CurrentTripHelpSupportPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(CurrentTripHelpSupportPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
